extends layout

block content
  h1= title
  -let items = results;

  div(class="container-fluid")
    div(class="row")
      each x in items
        div(class="col-sm-2") age: #{x.age} <br> size: #{x.size} <br> name: #{x.name} 